#include <stdio.h> 
int main (void)
{
    int number,i=0, z=0 , k=1 , temp =0 ;

scanf ("%d",& number);

if (number >= 0)
{

 for (i=1   ; i <= number   ; i++  ) 
 {
printf ("%d" , z );
printf ("\n");
temp = z + k ;
z = k ;
k = temp ;  
                                     }
return 0 ;
} 
}
